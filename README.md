 so the next part is the AI part which is very bad right now and we have to fix it at multiple places at multiple steps and I don't know what's the reason we are using a smaller smaller model so I think it is the reason that the accuracy is affecting, but again the model is good. Whatever model I'm using because when I was trying to query it separately, I'm getting some good results at least it's it's not as because the current bigger models, but we are getting some good more good results so before we I mean like we are testing it on the webpage also tested internally like for example give yourself a test query and see how it is responding because we have the pipeline somewhere running right the entire chat by plane like thinking and all so last time I manage to try to ask some questions but it failed very badly and I'm not sure that the reason that AI is working the alarm is working, but it's it's still failing so try testing it out from yourself. Try running a couple of I mean first put a pipeline so that in future also we can directly test it locally as same as the website one so. Do that and also since it's a small model try to see what is the best way you can use it what's the best way you can make it think because we are not having a limit limit on number of queries so keep it small and study like for example if if you want to gather some things, keep calling the AI multiple times and keep keep getting the results. Still keep making it in the results and also use tools as soon as possible. You don't have to go the entire data. You understand you put a scheme of data. You understand what the data offers understand what the data has and post that make use of tools like MCP's firstly make sure MCP is working fine because we don't have we don't want to go and read the database we have to or we don't. We don't want to go and read data directly from website. We have API we have a lot of things right make use of MCP's and sequel queries to run and get the data since schema. You don't understand what I'm talking about put some good context somewhere about what we handling about each and everything so that whenever I RA wants to differ something, it can easily go and fetch data from databases. it can do directly using sequel queries I mean think think that and also suggest me if there is any better plan you can think of here because I remember using small model. We had to be very efficient here and one more important thing keep it ready so that we can switch it to any different model any time actually don't do that. Don't do that. Will come back to it later right now let's stick to the current model and let's do it and the con should be very powerful very good. I think it should be very good again. We don't have any limited number of AI queries API queries to AILLM we can do as number as possible so keep calling multiple or the thinking should be very good. You can call it concurrent you can do it parallel call it multiple times at once to get the right amount of context before you answer something and please don't make it fail and make no mistakes. 